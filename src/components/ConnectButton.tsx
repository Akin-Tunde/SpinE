// src/components/ConnectButton.tsx

export function ConnectButton() {
  // Web components are global and don't require imports.
  // AppKit will automatically handle the connection state, address display, etc.
  return <appkit-button />;
}